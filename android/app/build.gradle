plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "de.posteo.nobo_code.music_up"  // Deine richtige Package ID!
    compileSdk = 34                          // UPGRADE: 34 → 36! 🚀
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8  // UPGRADE: Java 8 → 21! 🔥
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"                              // UPGRADE: Kotlin auch auf 21! ⚡
    }

    defaultConfig {
        applicationId = "de.posteo.nobo_code.music_up"  // Deine richtige App ID!
        minSdk = 21                                     // Bleibt bei 21 (Android 5.0+)
        targetSdk = 34                                 // UPGRADE: 34 → 36! 🎯
        versionCode = 5
        versionName = "1.3.1"
    }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk8:1.9.10"  // NEUESTE Kotlin Version!
}